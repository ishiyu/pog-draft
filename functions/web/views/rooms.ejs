<!DOCTYPE html>
<html lang="ja">
<head>
    <base href="<%= baseUrl %>" target="_self">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <title>ルーム一覧</title>

    <style>
        body {
            width: calc(100% - 30px);
            max-width: 360px;
            margin: 30px auto 0;
            font-size: 15px;
            background-color: #e8f5e9;
        }
        .input-wrapper {
            margin: 0 0 6px;
        }
        label {
            display: block;
            line-height: 20px;
        }
        input {
            box-sizing: border-box;
            width: 100%;
            border: 1px solid #bdbdbd;
            line-height: 20px;
            padding: 3px 6px;
        }
        textarea {
            box-sizing: border-box;
            width: 100%;
            height: 60px;
            border: 1px solid #bdbdbd;
            line-height: 20px;
            padding: 3px 6px;
            resize: none;
        }
        .submit-wrapper {
            margin-top: 18px;
            text-align: center;
        }
        button[type="submit"] {
            height: 30px;
            width: 120px;
            border: 1px solid #bdbdbd;
            line-height: 20px;
            background: #eee;
        }
    </style>
</head>
<body>
<h1>ルーム一覧</h1>
<ul>
    <% roomList.forEach((room, key) => { %>
    <li>
        <a href="rooms/<%= room.id %>/"><%= room.name %></a>
    </li>
    <%})%>
</ul>

<h1>ルーム作成</h1>
<form id="form" method="POST" action="rooms">
    <div class="input-wrapper">
        <label for="input-name">ルーム名</label>
        <input type="text" name="name" id="input-name">
    </div>
    <div class="input-wrapper">
        <label for="input-person">人数</label>
        <input type="number" name="person" id="input-person">
    </div>
    <div class="input-wrapper">
        <label for="input-limit">指名数</label>
        <input type="text" name="limit" id="input-limit">
    </div>
    <div class="submit-wrapper">
        <button type="submit">ルーム作成</button>
    </div>
</form>
</body>
</html>
